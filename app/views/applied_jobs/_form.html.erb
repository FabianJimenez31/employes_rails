<%= form_for(applied_job) do |f| %>
  <% if applied_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(applied_job.errors.count, "error") %> prohibited this applied_job from being saved:</h2>

      <ul>
      <% applied_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_employee %>
    <%= f.number_field :id_employee %>
  </div>

  <div class="field">
    <%= f.label :id_company %>
    <%= f.number_field :id_company %>
  </div>

  <div class="field">
    <%= f.label :id_vacancy %>
    <%= f.number_field :id_vacancy %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
